<div class="tile-container">
  <ul class="tile-list group">
    <% @pictures.each do |picture|%>
      <li class="tile">
        <%= link_to image_tag(asset_path(picture.filepath)), picture_url(picture) %>
      </li>
    <% end %>
  </ul>
</div>
